package co.com.atlas.model.access;

/**
 * Acción de acceso en portería.
 * ENTRY — Registro de entrada (QR o manual).
 * EXIT — Registro de salida de vehículo.
 */
public enum AccessAction {
    ENTRY,
    EXIT
}
